import React from 'react';
import { Typography, Box } from '@mui/material';

const Help = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h4">Help</Typography>
      <Typography variant="body1">This is a placeholder for the Help page.</Typography>
    </Box>
  );
};

export default Help;